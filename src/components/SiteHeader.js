import React from 'react';
import '../App.css';

const SiteHeader = () => (
    <section className="header">
        <header>
            <h1>Jake Peacock</h1>
        </header>    
    </section>
);

export default SiteHeader;